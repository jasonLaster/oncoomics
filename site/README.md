# Diana Omics Open Data

Vite landing page for the public `s3://diana-omics-raw-inputs-172630973301-us-east-1/diana/inbox/` dataset.

The file browser reads the current S3 inventory directly in the browser, so newly uploaded objects appear without rebuilding the site.

```bash
npm install
npm run dev
```
